{
  "name": "edutactile",
  "version": "0.5.0",
  "private": true,
  "description": "촉각 교육자료 제작 도구 — TGIL-quality tactile graphics for sighted creators, blind learners",
  "scripts": {
    "build": "node build.js",
    "test": "node test/ebrl-unit.js && node test/dotpad-sim.js && node test/dotpad-file-fallback.js && node test/e2e-feedback.js"
  },
  "dependencies": {
    "pako": "^2.1.0",
    "path2d-polyfill": "^2.0.1",
    "pdfjs-dist": "3.11.174"
  },
  "devDependencies": {
    "playwright": "^1.40.0"
  }
}
